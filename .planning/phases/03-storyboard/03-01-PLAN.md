# Plan: 03-01 - Test Scaffold and Schema

**Requirement(s):** STORY-01, STORY-03

**Context:** 
We need to define the exact data contract for the LLM output (which has no timing) and the final Timeline scene (which has timing). We also need the testing scaffold.

**Instructions:**
1. Create `storyboard/schema.py`.
2. Define `LLMSceneItem` (on_screen_text, visual_type: Literal["bullet", "image", "code"], visual_query).
3. Define `LLMScenesResponse` (scenes: list[LLMSceneItem]).
4. Define `TimelineScene` (idx, sentenceRange, start, end, onScreenText, visual).
5. Ensure `extra='ignore'` on `LLMSceneItem` config so it drops injected timing if the LLM hallucinates it.
6. Create `storyboard/tests/__init__.py`.
7. Create `storyboard/tests/conftest.py` with `sample_sentences` and `sample_timeline` fixtures.
8. Create `storyboard/tests/test_schema.py` to verify validation and that extra fields are dropped.

**Acceptance Criteria:**
- `pytest storyboard/tests/test_schema.py` passes cleanly.
