---
phase: 01-contracts-text-prep
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - tts/voice_store.py
  - tts/server.py
  - tts/colab_launch.py
autonomous: true
requirements:
  - INFRA-01
  - AUDIO-01
  - AUDIO-02
  - AUDIO-03
  - AUDIO-05

must_haves:
  truths:
    - "POST /synthesize {text, speaker_id, cfg_scale} with valid Bearer token returns 200 audio/wav non-empty"
    - "POST /synthesize with bad token returns 401; with text < 3 words returns 400"
    - "GET /voices returns a JSON list containing at least one string with 'Samuel' or 'indian' (case-insensitive)"
    - "POST /clone with a reference WAV uploads a voice; GET /voices then includes the new voice_id"
    - "voice_store.get_voice_path('default') returns a path that exists on disk after git clone (voices/default_indian.wav or VibeVoice/demo/voices/streaming_model/in-Samuel_man.pt)"
    - "Switching TTS backend (Colab → Modal) requires only changing the ENDPOINT_URL env var; the /synthesize contract (request shape, response format) is unchanged"
    - "colab_launch.py starts uvicorn in a background thread, starts cloudflared, and prints the public URL"
  artifacts:
    - path: "tts/voice_store.py"
      provides: "get_voice_path(speaker_id), list_voice_ids(), register_voice(voice_id, path)"
      exports: ["get_voice_path", "list_voice_ids", "register_voice"]
    - path: "tts/server.py"
      provides: "FastAPI app with POST /synthesize, GET /voices, POST /clone"
      exports: ["app"]
    - path: "tts/colab_launch.py"
      provides: "launch() — starts uvicorn + cloudflared, prints ENDPOINT_URL"
      exports: ["launch"]
  key_links:
    - from: "tts/server.py"
      to: "tts/voice_store.py"
      via: "server calls voice_store.get_voice_path(req.speaker_id)"
      pattern: "get_voice_path\\("
    - from: "tts/server.py"
      to: "VibeVoice/app.py (existing)"
      via: "run_vibevoice() reuses existing _generate_cpu / generate_gpu logic"
      pattern: "run_vibevoice\\|_generate_cpu\\|process_input_with_cached_prompt"
    - from: "tts/colab_launch.py"
      to: "tts/server.py"
      via: "uvicorn.run('tts.server:app', host='0.0.0.0', port=8000)"
      pattern: "tts\\.server:app"
---

<objective>
Implement the FastAPI TTS HTTP service (INFRA-01, AUDIO-01, AUDIO-02, AUDIO-03, AUDIO-05): voice registry, server endpoints, and Colab launch script.

Purpose: This is the stable HTTP contract that all future backends (Colab/Modal/RunPod) must honour. Changing the GPU host = changing one env var. The default Indian voice (`in-Samuel_man.pt`) is registered at startup so AUDIO-05 works immediately without a re-upload.
Output: tts/voice_store.py, tts/server.py, tts/colab_launch.py — the complete TTS service layer.
</objective>

<execution_context>
@C:/Users/Nxtwave/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Nxtwave/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-contracts-text-prep/01-RESEARCH.md
@.planning/phases/01-contracts-text-prep/01-01-SUMMARY.md
@VibeVoice/app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Voice store (AUDIO-02, AUDIO-03, AUDIO-05)</name>
  <files>tts/voice_store.py</files>
  <action>
    Implement voice registry abstracting 0.5B `.pt` prompts and 1.5B raw audio. Phase 1 delivers AUDIO-03 via 0.5B + the existing `in-Samuel_man.pt` Indian voice. Voice cloning from arbitrary audio requires the 1.5B fork (LOW confidence — see open question in 01-RESEARCH.md); implement a stub POST /clone for now with an upgrade note.

    `voice_store.py` must:

    1. **Auto-register built-in voices** at import time by scanning `VibeVoice/demo/voices/streaming_model/**/*.pt`. Uses the existing VOICES dict pattern from `VibeVoice/app.py`. The in-Samuel_man.pt file is already in that directory — confirmed.

    2. **Register default Indian voice** as speaker_id="default":
       - PRIMARY: `voices/default_indian.wav` at repo root (created by user or copied from in-Samuel_man.pt reference audio; documented below).
       - FALLBACK: if `voices/default_indian.wav` doesn't exist, resolve `in-Samuel_man.pt` path from `VibeVoice/demo/voices/streaming_model/` as the default. This covers the 0.5B path where `.pt` IS the cached prompt.
       - `get_voice_path("default")` returns whichever path exists.
       - Document as a comment: "AUDIO-05 persistence: voices/default_indian.wav should be committed to the repo so it survives git clone. If missing, falls back to in-Samuel_man.pt (0.5B). For arbitrary voice cloning from raw audio, upgrade to vibevoice-community/VibeVoice 1.5B fork."

    3. `list_voice_ids() -> list[str]`: returns ["default"] + the .pt stem names (e.g. "in-Samuel_man", "en-Carter_man", ...).

    4. `register_voice(voice_id: str, file_path: str) -> None`: adds to the in-memory registry. Used by POST /clone. Persists to `voices/{voice_id}.wav` (or .pt).

    5. `get_voice_path(speaker_id: str) -> str`: returns the path for the given id; raises KeyError if not found.

    Paths: use `pathlib.Path(__file__).parent.parent` to resolve repo root — don't hardcode absolute paths (Colab clones to /content/Audio or similar).
  </action>
  <verify>
    <automated>cd C:/Users/Nxtwave/Documents/Audio && pytest tts/tests/test_voice_store.py -x -v 2>&1</automated>
    <manual>test_default_indian_voice_exists passes — the path returned by get_voice_path("default") points to an existing file (in-Samuel_man.pt or voices/default_indian.wav).</manual>
  </verify>
  <done>All test_voice_store.py tests pass GREEN; get_voice_path("default") returns an existing file path; list_voice_ids() returns a non-empty list containing at least one voice with "samuel" in the name (case-insensitive).</done>
</task>

<task type="auto">
  <name>Task 2: FastAPI server (INFRA-01, AUDIO-01, AUDIO-02, AUDIO-03)</name>
  <files>tts/server.py</files>
  <action>
    Implement the FastAPI app. Read the existing `VibeVoice/app.py` first — reuse `_generate_cpu()` and `_load_local()` logic; do NOT rewrite the VibeVoice inference code.

    Exact endpoints (from 01-RESEARCH.md Pattern 1 — this shape is the permanent contract):

    ```
    POST /synthesize
    Authorization: Bearer <API_SECRET>
    Body: {"text": str, "speaker_id": str = "default", "cfg_scale": float = 1.5}
    → 200  Content-Type: audio/wav  (raw PCM16 WAV, 24kHz)
    → 400  {"detail": "Input too short (N words)"}
    → 401  {"detail": "Invalid token"}
    ```

    ```
    GET /voices
    → 200  ["default", "in-Samuel_man", "en-Carter_man", ...]
    ```

    ```
    POST /clone
    Content-Type: multipart/form-data
    Fields: voice_id: str, reference: UploadFile
    → 200  {"voice_id": "<id>", "message": "registered"}
    → 400  {"detail": "voice_id already exists"}
    ```

    Implementation notes:
    - `run_vibevoice(text, speaker_id, cfg_scale) -> np.ndarray` is the internal helper. For 0.5B: load .pt with `torch.load(..., weights_only=False)`; call `_processor.process_input_with_cached_prompt(text=text, cached_prompt=cached, ...)`. Reuse the pattern from `VibeVoice/app.py`'s `_generate_cpu()`. For tests, this function is mocked by conftest.py.
    - After run_vibevoice: encode to WAV with `soundfile.write(buf, audio_np, 24000, format='WAV', subtype='PCM_16')`.
    - Auth: `API_SECRET = os.environ.get("API_SECRET", "dev-secret")`. Use `HTTPBearer` from fastapi.security. In tests, conftest.py sets `API_SECRET=test-secret-123`.
    - Short-input guard: `if len(req.text.split()) < 3: raise HTTPException(400, ...)` — catches the case where normalizer was bypassed or input is too short. This guard is in addition to normalizer.py's guard.
    - POST /clone: save the uploaded file to `voices/{voice_id}.wav`; call `voice_store.register_voice(voice_id, path)`. Stub note: 0.5B cloning from raw audio is not supported — log a warning that cloning stores the file but the 0.5B model uses pre-computed .pt prompts; upgrade to 1.5B fork for true voice cloning.

    Write a module-level comment: "# ponytail: AUDIO-03 voice cloning via 0.5B stores reference audio but uses in-Samuel_man.pt for inference; true arbitrary cloning requires the vibevoice-community/VibeVoice 1.5B fork"
  </action>
  <verify>
    <automated>cd C:/Users/Nxtwave/Documents/Audio && pytest tts/tests/test_server.py -x -v 2>&1</automated>
    <manual>All 5 test_server.py tests pass GREEN with the mock_vibevoice fixture active.</manual>
  </verify>
  <done>All test_server.py tests pass GREEN; `from tts.server import app` works; the FastAPI app exposes /synthesize, /voices, /clone at the exact contracted shapes. server.py contains the ponytail comment: "AUDIO-03 via 0.5B stores reference audio only; true voice conditioning needs the 1.5B fork (upgrade path)."</done>
</task>

<task type="auto">
  <name>Task 3: Colab launch script (INFRA-01 — cloudflared tunnel)</name>
  <files>tts/colab_launch.py</files>
  <action>
    Implement `launch(port: int = 8000) -> str` that starts the FastAPI server in a background thread and starts a cloudflared quick tunnel, returning the public HTTPS URL.

    Exact pattern from 01-RESEARCH.md Pattern 2:
    ```python
    import subprocess, threading, time, uvicorn

    def launch(port: int = 8000) -> str:
        """Start FastAPI + cloudflared. Returns public HTTPS URL. Call once per Colab session."""
        def _run():
            uvicorn.run("tts.server:app", host="0.0.0.0", port=port, log_level="warning")
        threading.Thread(target=_run, daemon=True).start()
        time.sleep(3)  # wait for uvicorn

        proc = subprocess.Popen(
            ["cloudflared", "tunnel", "--url", f"http://localhost:{port}"],
            stderr=subprocess.PIPE, text=True
        )
        for line in proc.stderr:
            if "trycloudflare.com" in line:
                url = line.strip().split()[-1]
                print(f"ENDPOINT_URL = {url}")
                print("Paste into orchestrator: export ENDPOINT_URL=" + url)
                return url
        raise RuntimeError("cloudflared did not print a URL — is cloudflared installed?")
    ```

    Add a module-level docstring explaining: "Run this from a Colab cell after `git clone`. The returned URL is ephemeral (~90 min idle timeout). After a Colab restart, re-run launch() and update ENDPOINT_URL in the orchestrator. This is expected — see PROJECT_PLAN.md §6. For a permanent URL, deploy to Modal/RunPod with the same FastAPI server; only ENDPOINT_URL changes."

    Also: create `voices/` directory with a `.gitkeep` file (ensures directory exists after git clone even if no .wav is committed yet). Add a comment in voice_store.py fallback explaining the .gitkeep.

    No unit test needed for launch() — it requires a real Colab environment with cloudflared installed. The pattern is documented for manual Colab verification.
    # ponytail: colab_launch.py has no automated test; Colab env required
  </action>
  <verify>
    <automated>cd C:/Users/Nxtwave/Documents/Audio && python -c "from tts.colab_launch import launch; print('import OK')"</automated>
    <manual>On Colab after `pip install fastapi uvicorn cloudflared`: run `from tts.colab_launch import launch; url = launch()` → prints "ENDPOINT_URL = https://xxx.trycloudflare.com"; then `curl -H "Authorization: Bearer dev-secret" -X POST https://xxx.trycloudflare.com/synthesize -H "Content-Type: application/json" -d '{"text":"Hello world this is a test"}' -o out.wav` → out.wav is non-empty.</manual>
  </verify>
  <done>tts/colab_launch.py importable; voices/.gitkeep exists; module docstring documents the Colab ephemeral URL flow and Modal/RunPod upgrade path. All 3 plan tasks leave pytest tts/tests/ fully GREEN.</done>
</task>

</tasks>

<verification>
Run full Phase 1 test suite after all 3 tasks:

```bash
cd C:/Users/Nxtwave/Documents/Audio && pytest tts/tests/ -v --tb=short 2>&1
```

Expected: all tests GREEN (or SKIP on Windows for normalizer tests). test_server.py and test_voice_store.py must be GREEN.

Also verify schema import:
```bash
cd C:/Users/Nxtwave/Documents/Audio && python -c "from tts.schema import Timeline; from tts.normalizer import normalize; from tts.server import app; print('all imports OK')"
```
</verification>

<success_criteria>
- `pytest tts/tests/ -v` exits 0 (all pass or skip on Windows)
- `from tts.server import app` works; `/synthesize`, `/voices`, `/clone` routes are registered
- `voice_store.get_voice_path("default")` returns a path to an existing file (in-Samuel_man.pt or voices/default_indian.wav)
- `voices/.gitkeep` exists (ensures voices/ directory survives in git)
- tts/server.py has the ponytail comment about 0.5B AUDIO-03 limitation and 1.5B upgrade path
- tts/colab_launch.py has the module docstring about ephemeral URL + Modal/RunPod upgrade
</success_criteria>

<output>
After completion, create `.planning/phases/01-contracts-text-prep/01-03-SUMMARY.md`
</output>
