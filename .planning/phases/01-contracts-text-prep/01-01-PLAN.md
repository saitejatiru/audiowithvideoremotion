---
phase: 01-contracts-text-prep
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tts/tests/conftest.py
  - tts/tests/test_server.py
  - tts/tests/test_normalizer.py
  - tts/tests/test_voice_store.py
  - tts/tests/test_schema.py
autonomous: true
requirements:
  - INFRA-01
  - AUDIO-01
  - AUDIO-02
  - AUDIO-03
  - AUDIO-04
  - AUDIO-05

must_haves:
  truths:
    - "All 9 test stubs exist and fail (RED state) when run with `pytest tts/tests/ -x -q`"
    - "conftest.py provides: mock TTS function, 2-second 24kHz silence WAV bytes fixture, test API secret"
    - "test_normalizer.py covers AUDIO-04: currency expansion, short-input rejection, word_map population"
    - "test_server.py covers INFRA-01/AUDIO-01/AUDIO-02/AUDIO-03: /synthesize 200+WAV, 401 on bad token, /voices lists Indian voice, /clone registers new voice"
    - "test_voice_store.py covers AUDIO-05: default voice path exists on disk"
    - "Windows: normalizer tests are gated with pytest.mark.skipif(platform.system() == 'Windows')"
  artifacts:
    - path: "tts/tests/conftest.py"
      provides: "shared fixtures for all test files"
    - path: "tts/tests/test_server.py"
      provides: "RED stubs for INFRA-01, AUDIO-01, AUDIO-02, AUDIO-03"
    - path: "tts/tests/test_normalizer.py"
      provides: "RED stubs for AUDIO-04"
    - path: "tts/tests/test_voice_store.py"
      provides: "RED stubs for AUDIO-05"
    - path: "tts/tests/test_schema.py"
      provides: "RED stubs for timeline.json schema contract (inter-phase contract)"
  key_links:
    - from: "tts/tests/test_server.py"
      to: "tts/server.py (not yet written)"
      via: "from tts.server import app"
      pattern: "from tts\\.server import app"
    - from: "tts/tests/test_normalizer.py"
      to: "tts/normalizer.py (not yet written)"
      via: "from tts.normalizer import normalize"
      pattern: "from tts\\.normalizer import normalize"
---

<objective>
Write all test stubs for Phase 1 requirements in RED state (all failing) before any implementation code exists.

Purpose: TDD discipline — tests define the exact contract for every Phase 1 module. Executors of Wave 2 plans run `pytest tts/tests/ -x -q` after each file they write to confirm GREEN.
Output: 4 test files covering INFRA-01, AUDIO-01, AUDIO-02, AUDIO-03, AUDIO-04, AUDIO-05 — all failing.
</objective>

<execution_context>
@C:/Users/Nxtwave/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Nxtwave/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-contracts-text-prep/01-RESEARCH.md
</context>

<feature>
  <name>Phase 1 test scaffold</name>
  <files>tts/tests/conftest.py, tts/tests/test_server.py, tts/tests/test_normalizer.py, tts/tests/test_voice_store.py</files>
  <behavior>
    All tests fail on first run (RED). Tests define the contract; implementation fills it.

    conftest.py fixtures:
    - `mock_vibevoice(monkeypatch)` — patches `tts.server.run_vibevoice` to return a 2s float32 numpy array of zeros at 24kHz
    - `sample_wav_bytes()` — fixture that returns valid WAV bytes (2s silence, 24kHz, PCM16) using soundfile
    - `test_secret` = "test-secret-123"
    - `auth_headers` — {"Authorization": "Bearer test-secret-123"}
    - `monkeypatch` fixture that sets `API_SECRET` env var to test_secret

    test_server.py (uses httpx TestClient):
    - test_synthesize_returns_wav — POST /synthesize {"text": "Hello world this is a test", "speaker_id": "default"} → 200, Content-Type: audio/wav, len(body) > 0
    - test_auth_rejects_bad_token — POST /synthesize with Authorization: Bearer bad → 401
    - test_synthesize_short_input_rejected — POST /synthesize {"text": "hi"} → 400
    - test_voices_includes_indian — GET /voices → 200, response contains a string with "Samuel" or "indian" (case-insensitive)
    - test_clone_voice_registered — POST /clone multipart with reference WAV → 200, new voice_id appears in GET /voices

    test_normalizer.py (Windows skipif guard on all tests):
    - test_currency_expanded — normalize("I owe $42.50 today") → spoken_text has no "$" and no "42.50"; contains "dollars" or "cents"
    - test_short_input_rejected — normalize("ok") raises ValueError
    - test_word_map_populated — normalize("I owe $42.50 today") → word_map is a list with at least one entry; entry has keys "raw", "spoken", "start_word"
    - test_no_digits_in_spoken — normalize("The value is 25% complete today") → no bare digit tokens remain in spoken_text

    test_voice_store.py:
    - test_default_indian_voice_exists — voice_store.get_voice_path("default") returns a str; Path(that_str).exists() is True
    - test_list_voices_nonempty — voice_store.list_voice_ids() returns a non-empty list
    - test_list_voices_includes_default — "default" in voice_store.list_voice_ids() or any "samuel" (case-insensitive) in voice_store.list_voice_ids()

    test_schema.py (inter-phase contract — Phase 2 imports Timeline from here):
    - test_timeline_validates — Timeline.model_validate({"audio": {"path": "x.wav", "sampleRate": 24000, "durationSec": 1.0}, "meta": {"generator": "vibevoice-0.5B"}}) succeeds without raising; result.audio.durationSec == 1.0
    - test_write_phase1_timeline_structure — write_phase1_timeline("x.wav", 1.0, "vibevoice-0.5B", tmp_path/"t.json"); read back JSON; assert keys "audio", "words", "sentences", "scenes", "meta" present; words==[], sentences==[], scenes==[]; meta["generator"]=="vibevoice-0.5B"

    Windows skipif pattern for normalizer tests:
    ```python
    import platform
    pytestmark = pytest.mark.skipif(
        platform.system() == "Windows",
        reason="nemo-text-processing requires Linux (pynini has no Windows wheel)"
    )
    ```
  </behavior>
  <implementation>
    All test functions use `pytest.fail("not implemented")` or `assert False, "not implemented"` as body — except imports, which must reference the real module paths (tts.server, tts.normalizer, tts.voice_store). This ensures tests fail with ImportError or AssertionError on RED run, not syntax errors.

    Create `tts/__init__.py` and `tts/tests/__init__.py` as empty files so imports resolve.

    After writing all 5 test files, run `pytest tts/tests/ -q --tb=no` to confirm all tests collected and ALL fail. Commit the RED state: `test(01-01): add failing test stubs for INFRA-01 AUDIO-01..05`.
  </implementation>
</feature>

<verification>
<automated>cd C:/Users/Nxtwave/Documents/Audio && pytest tts/tests/ -q --tb=no 2>&1 | tail -5</automated>
<manual>Confirm output shows all tests collected and FAILED (not ERROR on syntax). ImportError on missing modules is acceptable — tests are RED as intended.</manual>
</verification>

<success_criteria>
- `pytest tts/tests/ --collect-only -q` lists all 11+ test functions without syntax errors
- `pytest tts/tests/ -q --tb=no` exits non-zero (all tests fail — expected RED state)
- All 6 requirement IDs (INFRA-01, AUDIO-01..05) are referenced in test docstrings or comments
- `tts/__init__.py` and `tts/tests/__init__.py` exist
</success_criteria>

<output>
After completion, create `.planning/phases/01-contracts-text-prep/01-01-SUMMARY.md`
</output>
